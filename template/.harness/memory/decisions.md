# Decisions

<!--
  Semantic memory (ADR-style). Review curates this from raw episodic history.
  You never delete a decision; you append a new one that supersedes it. Every
  agent reads the recent entries each cycle.

    ## Chose SQLite over Postgres for local dev
    why: zero-setup; the data volume is tiny; we can migrate later
    supersedes: (none)
-->

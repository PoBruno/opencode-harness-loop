# Inbox

<!--
  Raw intake dump — NO required structure from you. `desk` structures it; `groom`
  consumes it (prioritizes via WSJF and moves it to triaged/). Format (placeholder
  shown so this template is not miscounted):

    ## [I-{n}] type: {feature|bug|adjustment|modeling|vision} · type_confidence: {alta|low} · origin: chat {ts}
    "the raw thing you said, kept faithfully"

  A real item uses a literal id like [I-1]. There is no priority field (WSJF
  decides) and no approval field (nothing waits on you). Leave empty until desk
  adds work.
-->

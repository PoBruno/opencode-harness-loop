# Assumptions

<!--
  Semantic memory, low-confidence slice. Every assumption the system made under
  genuine ambiguity (two plausible readings, no invariant or prior decision to
  break the tie) is logged here, isolated on purpose so you can audit just the
  "educated guesses" without digging through everything. `planner` writes; you read
  when you want to correct one (via a new INBOX item — never a blocking reply).

    ## A-031 · TASK-142 · confidence: low · door: two-way
    Assumption: "export stats" means CSV, not Excel — the project's other exports use CSV.
    If wrong: trivial to add Excel later (additive).
-->

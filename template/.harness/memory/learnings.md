# Learnings

<!--
  Semantic memory: pitfalls already discovered, so no incarnation re-pays for a
  lesson. Review and Research append here; every agent reads it at the start of
  a cycle. Runtime incidents also land here.

    ## Library X hangs inside a transaction
    X.stream() must not be awaited inside a DB transaction — use the callback form.
-->
